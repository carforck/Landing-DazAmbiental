# URL del webhook de Google Apps Script vinculado al Sheet del cliente.
# Se lee solo en el servidor (src/app/api/enviar/route.ts): nunca llega al navegador.
# Sin esta variable el flujo funciona igual y la fila queda logueada en consola.
GOOGLE_SHEETS_WEBHOOK_URL=
